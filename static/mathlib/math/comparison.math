# MAX/MIN FUNCTIONS
CONST FN MAX(X, Y) = X > Y ? X : Y
CONST FN MIN(X, Y) = X < Y ? X : Y

# UNIT TESTS FOR FUNCTIONS
ASSERT(MAX(1, 2) == 2)
ASSERT(MAX(2, 1) == 2)
ASSERT(MIN(1, 2) == 1)
ASSERT(MIN(2, 1) == 1)