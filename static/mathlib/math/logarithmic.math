# LOGARITHMIC FUNCTIONS
CONST EXP = FN(X) E ^ X
CONST LOGN = FN(X, N) LOG(X) / LOG(N)
CONST LOG10 = FN(X) LOGN(X, 10)
CONST LOG2 = FN(X) LOGN(X, 2)

# UNIT TESTS
ASSERT(LOG(EXP(3)) == 3)
ASSERT(INT(LOGN(27, 3)) == 3)
ASSERT(LOG10(100) == 2)
ASSERT(LOG2(256) == 8)