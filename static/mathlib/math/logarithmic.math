# LOGARITHMIC FUNCTIONS
CONST FN EXP(X) = E ^ X
CONST FN LOGN(X, N) = LOG(X) / LOG(N)
CONST FN LOG10(X) = LOGN(X, 10)
CONST FN LOG2(X) = LOGN(X, 2)

# UNIT TESTS
ASSERT(LOG(EXP(3)) == 3)
ASSERT(INT(LOGN(27, 3)) == 3)
ASSERT(LOG10(100) == 2)
ASSERT(LOG2(256) == 8)